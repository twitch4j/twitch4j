# cid-workflow-version: 0.11.1

# This file is generated by the CID Workflow GitHub App.
# DO NOT EDIT!

# name
name: 'CI - Nightly'

# triggers
on:
  workflow_dispatch:
    inputs:
      loglevel:
        description: Log level
        required: true
        default: info
        type: choice
        options:
          - trace
          - debug
          - info
          - warn
          - error
  # cron-based trigger
  schedule:
    - cron: '27 0 * * 0'

# permissions, see https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#permissions and https://docs.github.com/en/rest/overview/permissions-required-for-github-apps
permissions:
  actions: read # required: detect GitHub Actions runtime environment
  checks: none # not required
  contents: read # required: read repository content
  deployments: none # not required
  id-token: none # not required
  issues: none # not required
  packages: none # not required
  pages: none # not required
  pull-requests: none # not required
  repository-projects: none # not required
  security-events: none # not required
  statuses: none # not required

# cancel in progress when a new run starts
concurrency:
  group: "${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}"
  cancel-in-progress: true

env:
  CID_WORKFLOW: 'main'
  CID_LOGLEVEL: "${{ github.event.inputs.loglevel || 'info' }}"
  # allowed modes are 'block' and 'audit'. Using https://github.com/step-security/harden-runner to harden the runner.
  EGRESS_POLICY: 'block'

# jobs
jobs:
  # gradle-build [twitch4j]
  gradle-build-twitch4j:
    name: 'gradle-build [twitch4j]'
    runs-on: ubuntu-24.04
    permissions:
      id-token: write # provenance signing
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          disable-telemetry: true
          egress-policy: "${{ env.EGRESS_POLICY }}"
          allowed-endpoints: >-
            repo.maven.apache.org:443
            repo1.maven.org:443
            kotlinlang.org:443
            plugins.gradle.org:443
            plugins-artifacts.gradle.org:443
            services.gradle.org:443
            downloads.gradle.org:443
            jcenter.bintray.com:443
            keyserver.ubuntu.com:443
            github.com:443
            api.github.com:443
            codeload.github.com:443
            uploads.github.com:443
            objects.githubusercontent.com:443
            raw.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            ghcr.io:443
            pkg-containers.githubusercontent.com:443
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Prepare Tooling
        shell: bash
        run: bash .cid/scripts/install.sh "0.11.1" "f565bcbfff61d8e242a2cd541842a695a0cea7ff585260400898404b93332951" "76A4948E69C62589C7B0AB84E414434DF5371FB6"
      - name: Action - gradle-build [twitch4j]
        env:
          CID_WORKFLOW: "${{ env.CID_WORKFLOW }}"
          CID_LOGLEVEL: "${{ env.CID_LOGLEVEL }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          WRAPPER_VERIFICATION: "${{ secrets.WRAPPER_VERIFICATION || vars.WRAPPER_VERIFICATION }}"
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-github.json" --state-wf-name "nightly" --step "gradle-build-twitch4j"
      - name: Upload Outputs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: "gradle-build-twitch4j-${{ github.run_id }}"
          path: ".dist/gradle-build-twitch4j/"
          retention-days: 1
          if-no-files-found: ignore
          compression-level: 6
          include-hidden-files: true
  # gradle-test [twitch4j]
  gradle-test-twitch4j:
    name: 'gradle-test [twitch4j]'
    runs-on: ubuntu-24.04
    permissions:
      id-token: write # provenance signing
      security-events: write # required: upload security events
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          disable-telemetry: true
          egress-policy: "${{ env.EGRESS_POLICY }}"
          allowed-endpoints: >-
            repo.maven.apache.org:443
            repo1.maven.org:443
            kotlinlang.org:443
            plugins.gradle.org:443
            plugins-artifacts.gradle.org:443
            services.gradle.org:443
            downloads.gradle.org:443
            jcenter.bintray.com:443
            keyserver.ubuntu.com:443
            github.com:443
            api.github.com:443
            codeload.github.com:443
            uploads.github.com:443
            objects.githubusercontent.com:443
            raw.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            ghcr.io:443
            pkg-containers.githubusercontent.com:443
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Prepare Tooling
        shell: bash
        run: bash .cid/scripts/install.sh "0.11.1" "f565bcbfff61d8e242a2cd541842a695a0cea7ff585260400898404b93332951" "76A4948E69C62589C7B0AB84E414434DF5371FB6"
      - name: Action - gradle-test [twitch4j]
        env:
          CID_WORKFLOW: "${{ env.CID_WORKFLOW }}"
          CID_LOGLEVEL: "${{ env.CID_LOGLEVEL }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-github.json" --state-wf-name "nightly" --step "gradle-test-twitch4j"
      - name: Upload Outputs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: "gradle-test-twitch4j-${{ github.run_id }}"
          path: ".dist/gradle-test-twitch4j/"
          retention-days: 1
          if-no-files-found: ignore
          compression-level: 6
          include-hidden-files: true
  # gitleaks-scan
  gitleaks-scan:
    name: 'gitleaks-scan'
    runs-on: ubuntu-24.04
    permissions:
      id-token: write # provenance signing
      security-events: write # required: upload security events
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          disable-telemetry: true
          egress-policy: "${{ env.EGRESS_POLICY }}"
          allowed-endpoints: >-
            keyserver.ubuntu.com:443
            github.com:443
            api.github.com:443
            codeload.github.com:443
            uploads.github.com:443
            objects.githubusercontent.com:443
            raw.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            ghcr.io:443
            pkg-containers.githubusercontent.com:443
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Prepare Tooling
        shell: bash
        run: bash .cid/scripts/install.sh "0.11.1" "f565bcbfff61d8e242a2cd541842a695a0cea7ff585260400898404b93332951" "76A4948E69C62589C7B0AB84E414434DF5371FB6"
      - name: Action - gitleaks-scan
        env:
          CID_WORKFLOW: "${{ env.CID_WORKFLOW }}"
          CID_LOGLEVEL: "${{ env.CID_LOGLEVEL }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-github.json" --state-wf-name "nightly" --step "gitleaks-scan"
      - name: Upload Outputs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: "gitleaks-scan-${{ github.run_id }}"
          path: ".dist/gitleaks-scan/"
          retention-days: 1
          if-no-files-found: ignore
          compression-level: 6
          include-hidden-files: true
  # semgrep-scan
  semgrep-scan:
    name: 'semgrep-scan'
    runs-on: ubuntu-24.04
    permissions:
      id-token: write # provenance signing
      security-events: write # required: upload security events
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          disable-telemetry: true
          egress-policy: "${{ env.EGRESS_POLICY }}"
          allowed-endpoints: >-
            semgrep.dev:443
            keyserver.ubuntu.com:443
            github.com:443
            api.github.com:443
            codeload.github.com:443
            uploads.github.com:443
            objects.githubusercontent.com:443
            raw.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            ghcr.io:443
            pkg-containers.githubusercontent.com:443
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Prepare Tooling
        shell: bash
        run: bash .cid/scripts/install.sh "0.11.1" "f565bcbfff61d8e242a2cd541842a695a0cea7ff585260400898404b93332951" "76A4948E69C62589C7B0AB84E414434DF5371FB6"
      - name: Action - semgrep-scan
        env:
          CID_WORKFLOW: "${{ env.CID_WORKFLOW }}"
          CID_LOGLEVEL: "${{ env.CID_LOGLEVEL }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          SEMGREP_RULES: "${{ secrets.SEMGREP_RULES || vars.SEMGREP_RULES }}"
          SEMGREP_APP_TOKEN: "${{ secrets.SEMGREP_APP_TOKEN }}"
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-github.json" --state-wf-name "nightly" --step "semgrep-scan"
      - name: Upload Outputs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: "semgrep-scan-${{ github.run_id }}"
          path: ".dist/semgrep-scan/"
          retention-days: 1
          if-no-files-found: ignore
          compression-level: 6
          include-hidden-files: true
  # trivyfs-scan
  trivyfs-scan:
    name: 'trivyfs-scan'
    runs-on: ubuntu-24.04
    permissions:
      id-token: write # provenance signing
      security-events: write # required: upload security events
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          disable-telemetry: true
          egress-policy: "${{ env.EGRESS_POLICY }}"
          allowed-endpoints: >-
            mirror.gcr.io:443
            keyserver.ubuntu.com:443
            github.com:443
            api.github.com:443
            codeload.github.com:443
            uploads.github.com:443
            objects.githubusercontent.com:443
            raw.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            ghcr.io:443
            pkg-containers.githubusercontent.com:443
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Prepare Tooling
        shell: bash
        run: bash .cid/scripts/install.sh "0.11.1" "f565bcbfff61d8e242a2cd541842a695a0cea7ff585260400898404b93332951" "76A4948E69C62589C7B0AB84E414434DF5371FB6"
      - name: Action - trivyfs-scan
        env:
          CID_WORKFLOW: "${{ env.CID_WORKFLOW }}"
          CID_LOGLEVEL: "${{ env.CID_LOGLEVEL }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-github.json" --state-wf-name "nightly" --step "trivyfs-scan"
      - name: Upload Outputs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: "trivyfs-scan-${{ github.run_id }}"
          path: ".dist/trivyfs-scan/"
          retention-days: 1
          if-no-files-found: ignore
          compression-level: 6
          include-hidden-files: true
