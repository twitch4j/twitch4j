mutation cancelPredictionEvent($input: CancelPredictionEventInput!) {
	cancelPredictionEvent(input: $input) {
		error {
			code
		}
		predictionEvent {
			channel {
				id
			}
			id
			status
		}
	}
}
