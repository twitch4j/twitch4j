mutation createPoll($input: CreatePollInput!) {
	createPoll(input: $input) {
		poll {
			id
			choices {
				id
				title
			}
			startedAt
			status
			title
			remainingDurationMilliseconds
		}
	}
}
